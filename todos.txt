notes on this file: priority = how breaking the change is x how central it is to homeapp/ark's philosophy (plain text, local-first, "the data is the index", generic files, minimal/command-driven apps).
  1 = breaking (incompatible with current data/workspaces) AND philosophy-critical
  2 = breaking, not philosophy-critical
  3 = non-breaking, but philosophy-critical
  4 = neither
not everything below is committed to - several are half-formed ideas, marked as such. see issues.txt for reports of current behaviour being wrong, as opposed to new work.

--- priority 1 ---

todo (TOP): pin down how groups and workspaces actually relate and how that should be presented to the user - this turned out to be bigger than either of the two things that surfaced it. Symptom 1: the topbar's app/group/workspace switcher (see issues.txt's navigation/topbar section) - redundant-feeling group vs. workspace selectors, runs out of space. Symptom 2: Files has no way to view multiple workspaces at once, unlike the old Documents app, which merged ark *records* (notes/todos/events) across every workspace a user toggled visible, filtered by tag, each entry labeled by origin workspace (core_groups.list_visible_workspaces/list_all_workspaces_with_visibility, deleted with Documents - see phase 3 in history below) - that only worked because Documents understood ark's tag/record syntax, which Files deliberately doesn't (opaque plain text, a directory tree, not records), so it's not a matter of re-adding the old toggle. Both symptoms trace back to the same unresolved question: what is a "workspace" to a user, how does it relate to a "group", and what does viewing/switching across several of either actually look like across every app - not just Files, not just the topbar. Explicitly bigger than originally scoped (2026-08-18) - come back to this before doing any more topbar or Files-visibility work, not scoped or designed yet.

todo: local-first storage architecture for media/ and appdata/. currently only documents/ (ark's data) is git-synced; media/ and appdata/ aren't touched by anything yet. open sub-questions:
  - sync mechanism for media/ - git is a poor fit for binary media (no meaningful diff/merge, full-history bloat without LFS). leaning toward a simpler rsync/syncthing-style whole-file mirror instead, not designed yet.
  - sync approach for appdata/ - likely per-app rather than universal (e.g. chat is probably server-authoritative, not offline-synced like documents/media).
  - chat specifically stores data at HOME_DATA_DIR/chat/<group_id>.txt today - scoped to group, not workspace - doesn't fit "one appdata per workspace" without deciding what happens when a group has multiple workspaces.
  - whether all app data (not just chat) should live inside a workspace's userspace, or apps can keep separate siphoned-off storage - undecided.
  - the underlying goal: a fully local (offline) version of homeapp should be possible, with all of a workspace's data (documents/media/appdata) sitting together in one place rather than scattered. this is the "why" behind all of the above.
priority 1 because any real answer here changes where data physically lives on disk (breaking for anything already written), and local-first is core to what homeapp is.

--- priority 2 ---

(nothing logged yet that's both breaking and philosophy-agnostic)

--- priority 3 ---

todo: Editor (see history below) should be a standalone app accessible in its own right, not just a HIDDEN hop reachable only via a file=/workspace= link from Ark or Files. Flagged as a real gap right after Editor shipped - needs its own entry in the launcher/app-switcher and a sensible landing experience when opened with no file= param yet (browse to a file first? pick a workspace? unclear, not designed).

todo: today ark only recognises files as its own within a dedicated ark repo (i.e. under a workspace's documents/ for an app=ark workspace) - anything outside that isn't treated as an ark file at all, regardless of extension. eventually: allow .ark-suffixed files to be recognised as ark files wherever they live, not just inside a dedicated ark repo - identified by the .ark extension itself rather than by location. distinct from the scrapped phase 2 flat-folder idea (see history below) - this is about where ark can find/recognise files, not about changing ark's own internal note/todo/evnt/yyyy/mm organisation inside a repo, which stays as decided. not scoped, exploratory.

todo: basic comment-like syntax convention for plain text files, in service of a possible future where ark can extract records embedded inside other file types (e.g. a .py file carrying an inline todo record) - separate, further-out idea than the .ark-suffix one above, not decided on.

todo: file editing permissions - even within a group, should anyone be able to edit any file, or is there a narrower per-file permission (e.g. assignee-only)? assignee (@) is an ark metadata concept though, so it won't obviously generalise past ark files. Editor (see history below) currently reuses Ark's existing coarse group-membership check unchanged - this is still open, not resolved by that.

todo: diffs and edit history for text files - show what changed and by whom, and let people look back through a file's history. documents/ is already git-backed per workspace, so this is plausibly "just" surfacing existing git history in the UI rather than new infrastructure.

todo: a unix-shell-style app for a workspace, connected to a separate "playground" for running code - distinct from Files (phase 3), which stays deliberately non-shell (see phase 3 notes for why Files doesn't invoke other apps' commands).

todo: plugin layer / mechanism for people to write and install their own apps, with real security boundaries around them. big, not scoped yet.

todo: search within Files, filename or content - Files has neither right now (see "Files simplified back down" in history below, which dropped both). Content search in particular tests "the data is the index" though, since a real content search has to stay a live scan rather than a background index, to stay consistent with ark's philosophy (see the phase 2 scrapped note in the history section for why an index isn't on the table). not scoped - if/how this comes back is open.

todo: what's the underlying interaction model for command-style apps in general (e.g. Ark's own terminal)? results currently show before any input in some places; help currently sits next to results rather than being the result of asking for help. these should follow one consistent rule instead of being decided per-screen. Files already resolved its own version of this (always shows the current directory's listing, since browsing is the whole point of that app) - this is about the rest of the app suite. same underlying question, folded in here: make the existing "interactive/clickable help" idea (_help.html command list becoming clickable) actually express whatever that rule ends up being, not just be clickable text.

todo: an app's on-disk data folder is named after the app itself (core/workspaces.py::path(), see history below) - what happens if that folder gets renamed or removed out from under HOME (e.g. by hand, or via Files/a future shell app browsing the workspace root one level up)? today nothing detects or recovers from this; the app would just look empty next time and silently recreate the folder on next write. needs a real answer - detect and refuse, detect and re-adopt, or something else - not scoped yet.

todo: let people see workspaces from more than one group at once, instead of one workspace at a time. related to the group/workspace switcher problems logged in issues.txt.

--- priority 4 ---

todo: chat - add live messaging (websockets/socket.io) so people don't have to refresh. visual redesign already shipped (log-style layout, coloured usernames, growing textarea, display_time) - this is just the realtime transport. means moving off sync gunicorn workers (e.g. -k eventlet) - a real infra change. issues.txt has the current "not receiving live" report confirming this is still outstanding.

todo: build a media viewer app, opening files under a workspace's media/ branch - launched from Files the same way Editor already is for text files.

todo: build a calendar app, linking to a workspace's ark evnt records.

todo: build a gantt view for todos - needs real start/duration data first; today timescales default purely off priority (!), and todos have no start-date concept. ark already has % (deadline) and >/< (evnt start/end).

todo: native app suite (iOS/Android, downloadable from the app stores) for the core apps - chat, files, editor, etc. - so people can interact with a workspace from a phone, not just a browser. sign-in flow: enter the server's domain, then username/password, same as the browser today. the browser version stays available regardless - this is an additional client, not a replacement. big, not scoped yet (how much is shared UI/logic vs native per-platform, which apps are "core" enough to get one, etc.).

todo: build a "postbox" app. not scoped.

todo: a suite of office-style apps. not scoped, and in real tension with homeapp's plain-text/minimal philosophy - worth being honest about that before sinking time into it.

todo: markdown rendering for notes/files - most natural home is the Editor app (see history below), not built yet.

todo: download option for files, wherever they're viewed (Editor, in practice - that's the shared surface everything opens a file through now).

todo: expose .arkrc settings in the Settings app, in an intuitive way - not just an editable dotfile. not scoped yet.

todo: label some apps as only available in multi-person groups, or not - undecided if this is even wanted.

todo: per-group app installs - let different groups enable/disable which apps they have available.

todo: topbar/mobile - check whether the app/group/workspace menu redesign (full-screen overlay menus, live search) already covers this, or a dropdown treatment is still wanted. see issues.txt for concrete problems with the current switcher.

todo: option to fully hide the topbar - not by default, unsure how.

todo: command history in the query box via up-arrow, shell-style.

todo: allow closing the output of a command, once shown.

todo: show the server address on the login page - unsure if actually wanted.

todo: chat - option to remove the topbar in chat specifically - unsure if actually wanted.

todo: revisit account "age" - unclear what this means yet (account age display? inactivity handling?), needs clarifying before it's actionable.

todo: a non-production version of the site to push changes to for testing, before pushing to production. not scoped yet - separate server/subdomain? separate data dir so test data doesn't mix with real workspaces? how does deploy actually promote staging -> production?

--- history (file-structure redesign) ---

phase 1 (DONE, 2026-08-17): inserted documents/media/appdata as three sibling branches inside each workspace. core/workspaces.py::path() now returns <root>/documents instead of <root> itself; new root() gives the true workspace root. ark keeps working exactly as it did before - same .txt extension, same note/todo/evnt dirs, vendored binary completely untouched - just nested one level deeper on disk. real server data migrates automatically and idempotently on server startup (core/storage.py::_migrate_documents_layout(), same "check current shape, fix if needed, run every boot" convention as core/db.py's own schema fixups) - no manual script to run. media/ and appdata/ are not eagerly created (nothing writes to them yet) and are not git-tracked - see priority 1 above. verified: migration idempotency + empty-workspace handling, full Flask-test-client flow (new workspace creation, save, lock, documents scan, enable_git puts .git inside documents/ correctly), and a real dev-server run against a seeded old-layout directory - zero errors, migration fired correctly on real startup.

correction to phase 1 (2026-08-18): phase 1's "documents/" was the wrong name for that sibling - it read as one generic bucket shared across apps, when the actual intent was that any app with user-facing data gets a folder named after itself inside the workspace (so ark's data lives under a workspace's own ark/, not documents/). core/workspaces.py::path() now returns root()/<app> instead of root()/documents; core/storage.py's boot-time migration handles both the original flat pre-phase-1 layout and the short-lived documents/ layout, folding either into <app>/, idempotently. media/ and appdata/ (still unimplemented, see priority 1) are unaffected by this - this was purely a naming/location fix for the one sibling actually in use.

phase 2: SCRAPPED (2026-08-18). Considered redesigning ark to drop the note/todo/evnt/yyyy/mm structure in favour of one flat, .arkrc-configurable output folder with date-first batch filenames. Rejected after realising two things: (1) most notes are multiline, and multiline notes each get their own file rather than being batched (only oneline records get consolidated) - so "flat" would mean thousands of individual files in one folder over time, not a small number of batches, which is exactly the "ever-growing flat folder" problem the redesign was trying to avoid; (2) type-as-directory (note/todo/evnt) and date-as-directory (yyyy/mm) are separate axes that got conflated during the design discussion - only the type axis was actually in tension with ark's "data is the index" philosophy, and a full flat layout traded that problem for a worse one (bulk human/terminal browsability) without a real win.

  decision: ark's internal file structure is staying exactly as it is, permanently. note/todo/evnt/yyyy/mm directories, .txt extension, filenames are ark-generated (yyyymmddThhmmss.txt) or the title metadata property if set - none of this changes. this is a closed design question now, not an open one - don't relitigate scan-performance or flat-folder ideas for ark's own directory layout without a genuinely new argument. "the data is the index" (no cache/index, full live scan every query) is still the permanent philosophy for ark, just realised via the existing note/todo/evnt/yyyy/mm tree rather than a flat one.

  vendored ark (apps/ark/vendor, submodule of benjaminingreens/ark.git) and the real upstream repo at /home/benjamin/ark need no changes as a result of this decision - phase 2 never got past planning, no code was touched.

phase 3 (DONE, 2026-08-18): replaced Documents with Files, and - as an extra, related piece of work decided mid-implementation - extracted a generic Editor app (apps/editor/, HIDDEN so it never shows in the launcher/switcher) out of what used to be Ark's own embedded file view/edit/save/lock code. The view/edit/save/lock/autosave/vim-mode machinery was never actually Ark-specific, it just used to live inside apps/ark/ - it's now shared infrastructure any app links a user into via a file=/workspace=/app=/home= URL (apps/editor/routes.py::editor_workspace() resolves off the workspace row's own `app` column, not a hardcoded "ark", so it's reusable beyond Ark's workspaces too). Ark's result-list links and Files' file rows both hand off to it now; Ark kept its own command/query engine and record-highlighted result-card rendering unchanged (colorize_meta, still Ark-only). Files (apps/files/) is a clickable directory listing (not a tag browser) - `mkdir`/`touch`/`mv`/`rm [-r]` typed commands for mutations, everything else typed is a case-insensitive filename search; never parses ark record syntax, treats every file as opaque plain text; scoped to a workspace's documents/ only (media/ isn't written to by anything yet); reuses Ark's existing workspace DB rows (app="ark") rather than minting its own, and Ark's own `ark_workspace()` resolver directly, both required for file_locks to correctly collide between Ark/Files/Editor editing the same file. `.ark`/`.git` (and any other dotfile) are filtered out of every listing, search, and command target. `/apps/ark/conflicts` (git conflict resolution) stayed in Ark, unchanged, out of scope. `templates/_topbar.html`'s old multi-workspace-visibility checklist (Documents-only, dead once Documents was deleted) was removed rather than repointed. verified end-to-end against a real dev-server run: Ark add/query/tidy still work, note content opens correctly through Editor with meta-highlighting correctly gone (plain text, no color), save/lock/unlock persist to disk, mkdir/touch/mv/rm/rm -r all work and correctly refuse a non-empty dir without -r, path-traversal and dotfile-mutation attempts both 403, filename search works, and the app list/topbar correctly show Ark/Chat/Files/Settings with Editor never appearing - zero errors in the server log.

Files simplified back down (DONE, 2026-08-18): the "make Files a real shell" rework (see the "Files as a real shell" entry above) had, in practice, produced two different visual languages for the same idea - typed command output (ls/cat/grep/find) in a separate #output viewer block, versus clicked results as the usual filename-card listing - which is the exact thing the earlier phase 3 design had deliberately avoided. Decided this was over-complication, not a step forward: reverted to a simple file browser again. cat/grep/find/ls/pwd are gone entirely, along with the subprocess/run_bin/COMMAND_TIMEOUT machinery that existed only to back them; the only typed commands left are cd (navigation) plus mkdir/touch/mv/rm[-r] (mutation, now implemented directly via pathlib/shutil rather than shelling out, since there's no longer a "real coreutils" shell-fidelity goal to justify the subprocess complexity). Every response (typed or clicked) now renders as the same filename-card listing of the current directory - no separate output view of any kind. Filename search (which the shell rework had replaced with real grep/find) was not brought back - Files currently has no search at all, typed or otherwise (see the content-search todo above). Also fixed in passing: a report that `rm` wasn't persisting to git turned out (near-certainly - not confirmed against production) to be an instance of the git-sync-scope-widening bug above: if the removed path lived outside the app's own data folder, under the old narrow git scope it was never tracked in the first place, so there was nothing for a delete to sync out. Verified end-to-end against a real dev-server run: mkdir/touch/mv/rm/rm -r all work, each correctly triggers auto_sync and lands in git history (confirmed via `git log`/`git ls-files`, not just "no error"), rm on a non-empty dir without -r is correctly refused, `..` navigation and /help both still work, zero errors in the server log.

git-sync-scope widened (DONE, 2026-08-18): git's scope was the app's own data folder only (e.g. a workspace's ark/) - anything sitting beside it at the true workspace root (docs/ - see the docs/-inside-ark/ incident, phase 3 above - or anything Files creates directly at the root) was completely outside git, never synced, never backed up. git now roots at the workspace root itself: core/workspaces.py::enable_git()/current_remote() operate on root() instead of path(); a new hoist_git_to_root() moves an existing <root>/<app>/.git up to <root>/.git and re-adds/commits (git represents this as a rename when content matches, so no history is lost, just one extra "moved under <app>/" commit) - called both by core/storage.py's boot-time migration (idempotent, every already-linked legacy workspace gets hoisted automatically on next server start) and by finish_link() (linking a pre-existing external workspace clones into <app>/ same as before, then hoists, so newly-linked workspaces end up in the same shape as everything else). apps/ark/routes.py, apps/editor/routes.py, apps/files/routes.py, core/app.py all updated to call auto_sync/is_git_linked with the root, not the app's data path - ark's own command execution (run/install/workspace_ready) is unaffected, still scoped to the app's own data folder, since that's unrelated to git. verified: a scratch legacy already-linked workspace hoists correctly and picks up a sibling docs/ folder into the same commit (idempotent on a second run); a fresh enable_git on a workspace with a sibling media/ folder includes it from the very first commit; the start_link/finish_link external-linking flow correctly hoists post-clone, preserving the external repo's own history; and a full dev-server HTTP run (Ark add, Files browse showing both ark/ and a legacy docs/ sibling, a Files mutating command triggering auto_sync that picks up and pushes docs/ alongside the new file, Editor opening both an ark/ file and a docs/ file) - zero errors in the server log.

--- dump ---

maybe change topbar to 'lowbar' - like a sort of bottom bar with any extra data, so that the screen can be clear for apps. and maybe add the command entry section just above bottom bar, idk. or maybe make this customisable?
